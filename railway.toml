[build]
builder      = "DOCKERFILE"
dockerfilePath = "Dockerfile"
buildContext   = "fastapi-be/api_service"

[deploy]
numReplicas             = 1
restartPolicyType       = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath         = "/health"
